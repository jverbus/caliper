# Demo fixtures

Canonical checked-in fixtures for demos live under:

- `docs/fixtures/workflow_demo/canonical/`
- `docs/fixtures/web_demo/canonical/`
- `docs/fixtures/email_demo/canonical/`

Service/embedded fixture duplication was intentionally pruned; regenerate backend-specific outputs from demo commands when needed.
